{% extends 'base.html.twig' %}

{% block title %}FollowMe{% endblock %}

{% block body %}
    <h1>FollowMe</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ follow_me.id }}</td>
            </tr>
            <tr>
                <th>Fontawesome</th>
                <td>{{ follow_me.fontawesome }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ follow_me.name }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('follow_me_index') }}">back to list</a>

    <a href="{{ path('follow_me_edit', {'id': follow_me.id}) }}">edit</a>

    {{ include('follow_me/_delete_form.html.twig') }}
{% endblock %}
