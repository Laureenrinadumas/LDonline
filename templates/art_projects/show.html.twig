{% extends 'base.html.twig' %}

{% block title %}ArtProjects{% endblock %}

{% block body %}
    <h1>ArtProjects</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ art_project.id }}</td>
            </tr>
            <tr>
                <th>ProjectName</th>
                <td>{{ art_project.projectName }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ art_project.picture }}</td>
            </tr>
            <tr>
                <th>Abstract</th>
                <td>{{ art_project.abstract }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('art_projects_index') }}">back to list</a>

    <a href="{{ path('art_projects_edit', {'id': art_project.id}) }}">edit</a>

    {{ include('art_projects/_delete_form.html.twig') }}
{% endblock %}
