{% extends 'base.html.twig' %}

{% block title %}WorkExperience{% endblock %}

{% block body %}
    <h1>WorkExperience</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ work_experience.id }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ work_experience.Title }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ work_experience.city }}</td>
            </tr>
            <tr>
                <th>Date</th>
                <td>{{ work_experience.date }}</td>
            </tr>
            <tr>
                <th>Abstract</th>
                <td>{{ work_experience.abstract }}</td>
            </tr>
            <tr>
                <th>TechnicalEnvironment</th>
                <td>{{ work_experience.technicalEnvironment }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('work_experience_index') }}">back to list</a>

    <a href="{{ path('work_experience_edit', {'id': work_experience.id}) }}">edit</a>

    {{ include('work_experience/_delete_form.html.twig') }}
{% endblock %}
