{% extends 'base.html.twig' %}

{% block title %}About{% endblock %}

{% block body %}
    <h1>About</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ about.id }}</td>
            </tr>
            <tr>
                <th>Age</th>
                <td>{{ about.age }}</td>
            </tr>
            <tr>
                <th>City</th>
                <td>{{ about.city }}</td>
            </tr>
            <tr>
                <th>Abstract</th>
                <td>{{ about.abstract }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('about_index') }}">back to list</a>

    <a href="{{ path('about_edit', {'id': about.id}) }}">edit</a>

    {{ include('about/_delete_form.html.twig') }}
{% endblock %}
